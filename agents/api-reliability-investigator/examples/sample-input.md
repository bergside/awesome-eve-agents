# Example request

POST /v1/imports returns intermittent 504 responses in production but succeeds in staging. Use the Postman collection and the last hour of traces and errors to narrow the failure domain and propose safe tests.

# Expected behavior

Follow the agent workflow, identify missing evidence, keep external writes in draft form, and end with the safest useful next action.
