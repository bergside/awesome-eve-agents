# Example request

The production research MCP server is healthy in Manufact, but agents started receiving tool discovery errors after a schema update. Correlate the deployment and telemetry, then propose a rollback or forward-fix decision tree.

# Expected behavior

Follow the agent workflow, identify missing evidence, keep external writes in draft form, and end with the safest useful next action.
